import type { Logger, SdkStore } from '../../../../lib/Sendbird/types';
interface Props {
    channelUrl: string;
    sdk: SdkStore['sdk'];
    logger: Logger;
    initialized: boolean;
    dependencies?: any[];
}
declare function useSetChannel({ channelUrl, sdk, logger, initialized, dependencies, }: Props): void;
export default useSetChannel;
