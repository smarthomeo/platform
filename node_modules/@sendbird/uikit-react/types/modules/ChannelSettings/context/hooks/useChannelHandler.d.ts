import type { SdkStore, Logger } from '../../../../lib/Sendbird/types';
interface UseChannelHandlerProps {
    sdk: SdkStore['sdk'];
    channelUrl: string;
    logger: Logger;
    forceUpdateUI: () => void;
    dependencies?: any[];
}
export declare const useChannelHandler: ({ sdk, channelUrl, logger, forceUpdateUI, dependencies, }: UseChannelHandlerProps) => any;
export {};
