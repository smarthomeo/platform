import type { Logger, SdkStore } from '../../../../lib/Sendbird/types';
interface MainProps {
    channelUrl: string;
    sdkInit: boolean;
}
interface ToolProps {
    sdk: SdkStore['sdk'];
    logger: Logger;
}
declare function useSetChannel({ channelUrl, sdkInit }: MainProps, { sdk, logger }: ToolProps): void;
export default useSetChannel;
