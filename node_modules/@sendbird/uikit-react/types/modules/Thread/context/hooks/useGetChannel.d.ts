import type { Logger, SdkStore } from '../../../../lib/Sendbird/types';
import { SendableMessageType } from '../../../../utils';
interface DynamicProps {
    channelUrl: string;
    sdkInit: boolean;
    message: SendableMessageType | null;
}
interface StaticProps {
    sdk: SdkStore['sdk'];
    logger: Logger;
}
export default function useGetChannel({ channelUrl, sdkInit, message, }: DynamicProps, { sdk, logger, }: StaticProps): void;
export {};
