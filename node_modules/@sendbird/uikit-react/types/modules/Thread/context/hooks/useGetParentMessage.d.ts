import type { Logger, SdkStore } from '../../../../lib/Sendbird/types';
import { BaseMessage } from '@sendbird/chat/message';
interface DynamicProps {
    channelUrl: string;
    sdkInit: boolean;
    parentMessage?: BaseMessage | null;
}
interface StaticProps {
    sdk: SdkStore['sdk'];
    logger: Logger;
}
export default function useGetParentMessage({ channelUrl, sdkInit, parentMessage, }: DynamicProps, { sdk, logger, }: StaticProps): void;
export {};
