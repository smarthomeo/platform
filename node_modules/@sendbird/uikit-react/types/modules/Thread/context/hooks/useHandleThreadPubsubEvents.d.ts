import { GroupChannel } from '@sendbird/chat/groupChannel';
import type { Logger } from '../../../../lib/Sendbird/types';
import { SBUGlobalPubSub } from '../../../../lib/pubSub/topics';
import { SendableMessageType } from '../../../../utils';
interface DynamicProps {
    sdkInit: boolean;
    currentChannel: GroupChannel | null;
    parentMessage: SendableMessageType | null;
}
interface StaticProps {
    logger: Logger;
    pubSub: SBUGlobalPubSub;
}
export default function useHandleThreadPubsubEvents({ sdkInit, currentChannel, parentMessage, }: DynamicProps, { pubSub, }: StaticProps): void;
export {};
