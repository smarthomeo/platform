import type { Logger, SdkStore } from '../../../../lib/Sendbird/types';
interface DanamicPrpos {
    sdk: SdkStore['sdk'];
}
interface StaticProps {
    logger: Logger;
}
export default function useGetAllEmoji({ sdk, }: DanamicPrpos, { logger, }: StaticProps): void;
export {};
