import { GroupChannel } from '@sendbird/chat/groupChannel';
import type { Logger } from '../../../../lib/Sendbird/types';
import { SendableMessageType } from '../../../../utils';
interface DynamicProps {
    currentChannel: GroupChannel | null;
    onMessageDeletedByReqId: (reqId: string | number) => void;
    onMessageDeleted: (channel: GroupChannel, messageId: number) => void;
}
interface StaticProps {
    logger: Logger;
}
export default function useDeleteMessageCallback({ currentChannel, onMessageDeletedByReqId, onMessageDeleted, }: DynamicProps, { logger, }: StaticProps): (message: SendableMessageType) => Promise<void>;
export {};
