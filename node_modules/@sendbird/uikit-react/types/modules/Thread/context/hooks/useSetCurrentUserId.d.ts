import type { User } from '@sendbird/chat';
interface DynamicParams {
    user: User | null;
}
declare function useSetCurrentUserId({ user }: DynamicParams): void;
export default useSetCurrentUserId;
