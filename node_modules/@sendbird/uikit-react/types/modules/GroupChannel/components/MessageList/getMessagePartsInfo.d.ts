import { GroupChannel } from '@sendbird/chat/groupChannel';
import { CoreMessageType } from '../../../../utils';
import { StringSet } from '../../../../ui/Label/stringSet';
export interface GetMessagePartsInfoProps {
    allMessages: Array<CoreMessageType>;
    stringSet: StringSet;
    isMessageGroupingEnabled?: boolean;
    currentIndex: number;
    currentMessage: CoreMessageType;
    currentChannel?: GroupChannel | null;
    replyType?: string;
}
interface OutPuts {
    chainTop: boolean;
    chainBottom: boolean;
    hasSeparator: boolean;
}
/**
 * exported, should be backward compatible
 */
export declare const getMessagePartsInfo: ({ allMessages, stringSet, isMessageGroupingEnabled, currentIndex, currentMessage, currentChannel, replyType, }: GetMessagePartsInfoProps) => OutPuts;
export {};
